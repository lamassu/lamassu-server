const seconds = 1000
const second = seconds
const minutes = 60 * seconds
const minute = minutes
const hours = 60 * minutes
const hour = hours
const days = 24 * hours
const day = days
const weeks = 7 * days
const week = weeks
const year = 365 * days
const years = year

module.exports = {
  seconds,
  second,
  minutes,
  minute,
  hours,
  hour,
  days,
  day,
  weeks,
  week,
  years,
  year
}
